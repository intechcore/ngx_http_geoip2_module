# An invalid auto_reload interval in the http block. nginx -t must reject it.
load_module modules/ngx_http_geoip2_module.so;
events {}
http {
    geoip2 /fixtures/a.mmdb {
        auto_reload bogus;
        $geoip2_country country iso_code;
    }
}
