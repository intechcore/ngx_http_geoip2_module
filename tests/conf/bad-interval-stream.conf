# An invalid auto_reload interval in the stream block. nginx -t must reject it.
load_module modules/ngx_stream_geoip2_module.so;
events {}
stream {
    geoip2 /fixtures/a.mmdb {
        auto_reload bogus;
        $geoip2_country country iso_code;
    }
}
